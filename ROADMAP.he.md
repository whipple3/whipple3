# whipple3 — רואדמאפ מלא (עברית)

מסודר לפי **תלות, לא לפי לוח שנה**. כל שלב מפרט מה נבנה, מה פירוש "גמור",
ומה אסור שידלוף פנימה. שלבים 0–4 הם המוצר. 5 והלאה הם אופציונליות — הם קורים
רק אם השלב שלפניהם הצדיק אותם.

התכן הקנוני: `SPEC.md`. הגרסה האנגלית (המחייבת): `ROADMAP.md`.

---

## שלב 0 — השלד ✅ הושלם

התשתית שכל השאר נשען עליה, ירוקה מקצה לקצה.

- **`@whipple3/core`** — ליבה פונקציונלית טהורה: branded IDs, ‏`GraphState`
  אימיוטבילי, רדיוסר `apply()` עם optimistic versioning ו־claim/lease, בדיקות
  ACL, חיתוך קונטקסט (`neighborhood`), טקסונומיית אירועים מלאה, `Result`.
  תלות ריצה אחת: Zod.
- **`@whipple3/log`** — פורט `LogStore`/`ReadonlyLog` עם אדפטרים memory
  ו־NDJSON, וסוויט קונפורמנס משותף שרץ נגד שניהם.
- **`@whipple3/transport-mcp`** — סכמות Zod לכלי הלוח.
- שלד ה־CLI ודוגמת פלאגין ל־Claude Code (סורק / מבקרים / מתקן + `/audit`).
- שערי CI: ‏tsc קשיח, Biome, ‏dependency-cruiser (טוהר הליבה וכיוון imports),
  ‏Vitest + אינווריאנטים ב־fast-check.

**"גמור" אומר:** `pnpm typecheck && lint && depcruise && test && build` — הכול ירוק.

---

## שלב 1 — הכתיבה הראשונה לגרף ✅ הושלם

הרגע שבו זה מפסיק להיות מסמכים.

- **הקשחת זהות** — ‏`agentId` לא קיים בשום payload של כלי; הזהות נגזרת
  מהחיבור עצמו. `principal` נכנס ל־`EventMeta` פעם אחת בתחילת הסשן —
  ייחוס שאי־אפשר לשחזר בדיעבד, עכשיו־או־לעולם־לא.
- **ACL בשני הכיוונים** — כתיבה *וגם* קריאה; הסינון קורה תוך כדי חציית הגרף,
  כך שצומת שאסור לקרוא לא דולף אפילו כקצה של קשת. כל דחייה נרשמת כאירוע
  `acl.denied` — אכיפה משאירה שובל, לעולם לא נפילה שקטה.
- **`session.ts`** — הקליפה האימפרטיבית: מחזיקה `GraphState`, ‏`LogStore`,
  ‏`AclPolicy`; מזריקה זמן ו־ULID; צינור אחיד: parse (Zod) ← ‏`checkAcl` ←
  ‏`apply` ← ‏`append` ← תשובה בצורת `Result`.
- **שרת MCP על stdio** עם שישה כלים
  (`post` / `read` / `claim` / `release` / `next` / `status`) — שגיאות
  מובנות בלבד, אף פעם לא פרוזה.
- **`blackboard_next`** — שאילתת pull: צמתים לפי label + props, בלי צמתים
  תחת claim חי, ורק בתוויות שמותר לאייג'נט לקרוא.
- טסטים אינטגרטיביים ברמת הסשן: מריצים את כל ההנדלרים ובודקים גם את תוכן
  הלוג וגם את המצב שנוצר.

**"גמור" אומר:** סאב־אייג'נט מפרסם צומת, שני תובע אותו, והלוג מראה את שניהם
עם זהות וסיבתיות נכונות.
**הריצה החיה על מארח אינטראקטיבי** (זימון סאב־אייג'נטים, פרומפטים של הרשאות)
היא סעיף ברשימת ההשקה — `docs/launch/checklist.md`.

---

## שלב 2 — עבודה מקבילית בלי התנגשויות ✅ הושלם

טענת התיאום, מוכחת.

- שלושה מבקרים במקביל על לוח אחד: claims, פקיעת lease על נטישה,
  אפס עבודה כפולה, אפס עדכונים אבודים.
- מתקן (fixer) מאחורי שער אישור אנושי לפני כל כתיבה לקובץ.
- **מחזור החיים התלת־שכבתי:** `distill()` ← ‏`report.md`; הגרף מתפנה;
  לוג הטרייס נשמר.
- ליטוש ה־DSL של הסכמות: הסקת literals מלאה כך ש־`when({ status: "pendig" })`
  הוא שגיאת קומפילציה. כאן הוצא תקציב ההתעמלות הטיפוסית.
- כוונון slices: הצהרת slice לפי תפקיד בסכמה, במקום עומק BFS גולמי.
- **הוקדם משלב 5:** לוח משותף על Unix domain socket‏ (`@whipple3/transport-uds`
  + ‏`whipple3 serve`) — תהליך אחד מחזיק את הסשן והלוג; חיבור לכל אייג'נט;
  זהות קשורה לסוקט בזמן ההתחברות; זהות אחת מחזיקה לכל היותר חיבור חי אחד.

**"גמור" אומר:** ריצות מקביליות חוזרות עם אפס claims כפולים ואפס עדכונים
אבודים, והריצה מסתיימת בלי אורקסטרטור שמנטר אותה.

---

## שלב 3 — סטודיו: הדבר שעושים לו צילום מסך ✅ הושלם בעיקרו

מנוע האימוץ. השלב שמכריע אם מישהו בכלל ישמע על whipple3.

- Vite + ‏TS ונילי + ‏sigma.js על graphology, ניזון מ־SSE מתוך `ReadonlyLog`.
  ‏graphology חי רק כאן.
- גרף חי: צמתים מופיעים כשאייג'נטים מפרסמים, צבעים משתנים עם הסטטוס,
  claims צבועים לפי האייג'נט המחזיק.
- קליק על צומת ← היסטוריית המוטציות שלו והאייג'נט שייצר אותן.
- **סקרבר מסע־בזמן** — שחזור הסשן מהלוג. תוצאה חינמית של הרדיוסר הטהור,
  והפיצ'ר הכי דמו־אבילי בפרויקט.
- **נשאר:** חיווט הפקודות `whipple3 studio` ו־`whipple3 replay <log>` —
  היום הסטודיו רץ דרך dev של החבילה.

**"גמור" אומר:** הקלטת מסך של 20 שניות שגורמת לזר להבין את המוצר בלי קריינות.

---

## שלב 4 — הוכחה והשקה ⟵ כאן אנחנו עכשיו

טענות הופכות למדידות. צד הקוד ברובו בנוק; הרשימה המחייבת והמסודרת לפי תלות:
`docs/launch/checklist.md`.

- **בנצ'מרק:** אותה משימת audit — ‏whipple3 מול סאב־אייג'נטים רגילים שהכול
  אצלם מסתכם דרך קונטקסט ההורה. מודדים: טוקנים של קונטקסט האורקסטרטור, זמן
  קיר, איכות ממצאים, עבודה כפולה. יעד: ‎≥40%‎ חיסכון בטוקנים באיכות שווה.
  ההרנס בנוי וריצה חיה בצד whipple3 בנוקה; חסרות ריצות ההשוואה והמספרים
  הסופיים. **מפרסמים בכנות, כולל איפה שמפסידים.**
- **ולידציה חיה:** ריצת `/whipple3:audit` על Claude Code אינטראקטיבי —
  הדבר היחיד שטסטים headless לא מוכיחים — והקלטת הסטודיו במהלכה.
- **דוקומנטציה:** README עם quickstart של 10 דקות, ARCHITECTURE, ‏ADRs,
  רפרנס API.
- **אריזה:** הפלאגין ארוז כמו שצריך; נעיצת גרסת Claude Code מינימלית.
- **משפטי ומנהלי:** שם משפטי מלא ב־LICENSE, ארגון GitHub וארגון npm,
  הכרעת DCO מול CLA לפני קבלת תרומות חיצוניות משמעותיות.
- **פרסום:** publish ל־npm (נועל את השם), תיוג v0.1.0, שני פוסטים —
  אנגלית ועברית.

**"גמור" אומר:** זר מתקין תוך פחות מ־10 דקות, ויש מספרים להתווכח עליהם.

---

## שלב 5 — עצמאות מהמארח (host-agnostic) — התחיל

להפוך את "לא נעולים לאף ספק" מטענה להקלטה.

- אותו שרת מול Codex CLI, ‏Gemini CLI, ‏opencode, ‏Cursor — עמוד תוצאות לכל
  מארח (`docs/hosts/`). טבלת התאימות כבר ב־README, שורות כנות בלבד; מה שחסר
  הוא ריצות מלאות מאחורי אימות אמיתי בכל מארח.
- ~~UDS transport~~ — הוקדם ונבנה בשלב 2.
- פורט `SandboxProvider`: אדפטר dockerode קודם; E2B / Daytona ודומיהם אחריו.
  **רוכבים על שכבת הביצוע, לא בונים אותה מחדש.**

**"גמור" אומר:** טבלת תאימות שבה כל שורה היא ריצה אמיתית.

---

## שלב 6 — מצב Push: ‏whipple3 מריץ את הלולאה

המעבר מ"קשר רדיו" לשיבוץ אוטומטי — מהמונית שמקשיבה לתחנה ל־Gett שמשבץ לבד.
שווה לבנות רק אם שלבים 1–4 מצאו משתמשים.

- **סקדולר:** התאמה ← תורים פר־אייג'נט ← שיגור; תקרות מקביליות, batching,
  ‏backpressure. ‏XState למכונת המצבים של חיי אייג'נט
  (idle ← ‏triggered ← ‏working ← ‏done/failed).
- **סמנטיקת כשלים:** retries עם אידמפוטנטיות, פקיעת lease, ‏dead-letter,
  תקציבי hops וזיהוי מעגלים מתוך שרשרת הסיבתיות.
- **זיהוי שקט (quiescence):** כל האייג'נטים בטלים ∧ התורים ריקים ∧ כלום
  באוויר ∧ חלון התייצבות — הגרסה הכנה של "המשימה נגמרה".
- פורט `ModelProvider` דרך Vercel AI SDK ← ‏OpenRouter / כל ספק. ‏BYOM,
  כולל ציים של מודלים זולים: slices קטנים ומוקלדים + ולידציית סכמה מורידים
  את רף האינטליגנציה הנדרש פר־אייג'נט.
- `when()` מתקמפל למנויי push אמיתיים; אותה סכמה משרתת את שני מצבי השיגור.

**"גמור" אומר:** סשן שרץ עד הסוף בלי אורקסטרטור חיצוני, ושורד אייג'נט
שנהרג באמצע משימה.

---

## שלב 7 — Observability ו־Evals

פולטים, לא אוגרים. שניהם אדפטרים — כי טקסונומיית האירועים נשמרה מהיום הראשון.

- **`@whipple3/otel`:** ‏spans לפי מוסכמות GenAI של OTel ← ‏Langfuse /
  Jaeger / קונסול.
- **`@whipple3/evals`**, שלוש שכבות:
  1. **טסטים של replay דטרמיניסטי** — לוגים של סשנים כ־fixtures + הקלטה
     ושידור־חוזר של קריאות LLM בסגנון VCR; רגרסיות תיאום נתפסות ב־CI בלי
     קריאת API אחת.
  2. **אסרשנים על טרייקטוריות של מצב מוקלד** — השכבה החדשנית:
     `expect(final).toSatisfy(CodeFile.all({ status: "audited" }))`,
     ‏`expect(trace).maxHops(5)`, תקרת עלות לסשן. כלי eval קיימים שופטים
     טקסט; whipple3 שופט טרייקטוריות של מצב מובנה.
  3. **LLM-as-judge** על הפלט המזוקק — קומודיטי, ממשק scorer פתוח.
- אדפטר לוג SQLite (‏`node:sqlite` המובנה), עובר את אותו סוויט קונפורמנס.

**"גמור" אומר:** רגרסיית תיאום נתפסת ב־CI לפני שבן־אדם שם לב.

---

## שלב 8 — אקוסיסטם

הפרויקט מפסיק להיות של אדם אחד.

- תבניות סכמות ואייג'נטים מעבר ל־code audit: פייפליני מחקר, עבודות תוכן,
  ‏QA של דאטה, migration sweeps.
- אדפטרים כך ש־whipple3 שמיש **מתוך** Mastra / AgentKit — לא במקומם.
- מסלול תורמים: good-first-issues, משמעת ADR, קצב releases עם changesets.
- הרצאות וכתיבה: אנלוגיית Redux לקהל אחד, "Gett של הרתמות" לקהל אחר.

**"גמור" אומר:** PR משמעותי נוחת ממישהו שמעולם לא פגשת.

---

## שלב 9 — אופציונליות מסחרית

רק אם שלב 8 הביא צוותים אמיתיים. **שום דבר מכאן לא נכנס לריפו ה־MIT** —
החפיר הוא המשמעת, לא הרישיון.

הקו הוא לא "כמה" אלא **אחד מול רבים**. הליבה נשארת שלמה ובלתי־מוגבלת לנצח.

| חינם לנצח (OSS) | מסחרי |
|---|---|
| סשן מקומי, לוג על דיסק | לוגים של כל הצי: מפתחים + CI במקום אחד |
| `canMutate` בקובץ בריפו | הפצת מדיניות מרכזית לכל סשן |
| `agentId` מאומת מול החיבור | SSO ← מיפוי תפקידים, service accounts |
| `principal` מהסביבה המקומית | on-behalf-of מאומת, זהות ארגונית |
| שובל של סשן בודד | **תצוגת פעילות חוצת־סשנים לפי זהות** |
| טסטים של replay מקומיים | סוויטות רגרסיה של טרייקטוריות ברמת צוות |
| סטודיו מקומי | Retention, חיפוש, ייצוא SIEM, שיוך עלויות |

**הדבר הראשון שנמכר:** לא פלטפורמה — דשבורד אחד שעונה על *"מה האייג'נטים
שלנו עשו השבוע, תחת איזו מדיניות, ומה נחסם."* הקונה: מוביל פלטפורמה /
AI-infra, עם security מאחוריו. הוא נמכר בגלל נקודת החנק שכבר בקוד: כל שינוי
מצב עובר `checkAcl` ← ‏`apply` ← ‏`append`. פריימוורקים שבהם אייג'נטים מדברים
בטקסט חופשי לא יכולים למכור governance — אין להם נקודת אכיפה.

המסלול: אימוץ ← design partners ← מוצר. לעולם לא הפוך. הכנסה בינתיים, אם
בכלל — ייעוץ וחסויות; המטרה הראשית נשארת תעסוקה.

---

## חוקים קבועים לכל השלבים

1. **הליבה נשארת טהורה.** בלי I/O, בלי שעונים, בלי אקראיות, בלי imports של
   `node:`. ‏CI אוכף.
2. **הלוג הוא מקור האמת.** כל יכולת במורד הזרם — סטודיו, replay, ‏evals,
   ‏audit — תלויה בזה ובשום דבר אחר.
3. **לא בונים את שכבת הביצוע.** Sandboxes, שערי credentials, ראנטיימים של
   מודלים: אדפטרים בלבד.
4. **אין שפת שאילתות מול LLM.** כלים מוקלדים ומוגבלים בלבד.
5. **הגרף הוא מישור הבקרה.** נתיבים ו־hashes, לעולם לא blobs.
6. **כל תלות היא החלטה.** בליבה: Zod בלבד. בכל מקום אחר: הצדק או אל תוסיף.
7. **אבסטרקציה רק בפורטים.** Rule of three בכל מקום אחר. הליבה מתחת
   ל־1,000 שורות בערך; אף קובץ מעל 200 בערך.
8. **שום דבר בעל פוטנציאל מסחרי לא יוצא תחת MIT.** מה ששוחרר — שוחרר.
