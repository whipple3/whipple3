/**
 * TODO(W1): wire the stdio MCP server using @modelcontextprotocol/sdk —
 * register the five tools from tools.ts, validate with their Zod schemas,
 * run mutations through checkAcl() + apply(), append to the LogStore,
 * and return structured Result-shaped errors (never prose). (SPEC §6)
 *
 * Kept dependency-free in the skeleton so the whole repo typechecks green
 * before the SDK wiring lands.
 */
export const notYetImplemented = "transport-mcp: W1";
